<UserControl x:Class="TTC.CadTools.AutoCAD.UI.StatusControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             Background="#252526" Foreground="#F1F1F1" FontFamily="Segoe UI" FontSize="12">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="TTC CAD Foundation" FontWeight="SemiBold" FontSize="14" Foreground="#4EC9B0" Margin="0,0,0,8"/>
        <Separator Grid.Row="1" Background="#3E3E42" Margin="0,0,0,12"/>

        <!-- Diagnostic State -->
        <StackPanel Grid.Row="2">
            <TextBlock Text="Document State:" FontWeight="SemiBold" Foreground="#9CDCFE" Margin="0,0,0,2"/>
            <TextBlock x:Name="TxtDocument" Text="Checking..." Foreground="#DCDCDC" Margin="0,0,0,10" TextWrapping="Wrap"/>

            <TextBlock Text="Configuration:" FontWeight="SemiBold" Foreground="#9CDCFE" Margin="0,0,0,2"/>
            <TextBlock x:Name="TxtConfiguration" Text="Loading..." Foreground="#DCDCDC" Margin="0,0,0,10" TextWrapping="Wrap"/>

            <TextBlock Text="Active Log:" FontWeight="SemiBold" Foreground="#9CDCFE" Margin="0,0,0,2"/>
            <TextBlock x:Name="TxtLogPath" Text="Initializing..." Foreground="#DCDCDC" Margin="0,0,0,10" TextWrapping="Wrap" FontSize="11"/>
        </StackPanel>

        <!-- Footer -->
        <StackPanel Grid.Row="3" Margin="0,8,0,0">
            <Separator Background="#3E3E42" Margin="0,0,0,8"/>
            <TextBlock x:Name="TxtVersion" Text="Version: 1.0.0.0 (F0)" Foreground="#858585" FontSize="11"/>
        </StackPanel>
    </Grid>
</UserControl>
